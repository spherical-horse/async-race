import BaseComponent from '../../../baseComponent';

export default class WinnersTableBody extends BaseComponent {
  constructor(parentNode: HTMLElement) {
    super('tbody', [], parentNode, '', {});
  }
}
