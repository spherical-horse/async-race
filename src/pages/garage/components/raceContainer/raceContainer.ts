import BaseComponent from '../../../baseComponent';

export default class RaceContainer extends BaseComponent {
  constructor(parentNode: HTMLElement) {
    super('div', ['race-container'], parentNode, '', {});
  }
}
